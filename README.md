# hackapet-node-mongodb

Código de exemplo do workshop de APIs HTTP com NodeJS e MongoDB para o Hackapet do CIn - UFPE

### Para rodar
Para rodar essa aplicação, com docker e docker-compose instalados, rodar:
* ```docker-compose up```
